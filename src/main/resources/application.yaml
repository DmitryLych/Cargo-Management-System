server:
  port: 9090

#DataBase
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Cargo_Managament_System_DB
    username: Dmitry
    password: Dmitry
  jpa:
    hibernate:
      ddl-auto: validate

#Flyway
flyway:
  enabled: true
  locations: classpath:db/migration
  check-location: true
  baseline-on-migrate: true